<?xml version="1.0"?>
<ruleset name="Installment Prices for WooCommerce">
    <description>PSR-12 formatting plus WordPress security and correctness sniffs.</description>

    <file>foundation/src</file>
    <file>modules/admin-ui/src</file>
    <file>src</file>
    <file>woocommerce-parcelas.php</file>
    <file>uninstall.php</file>

    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/public/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>

    <arg name="extensions" value="php"/>
    <arg name="colors"/>
    <arg value="ps"/>
    <arg name="parallel" value="8"/>

    <config name="testVersion" value="8.2-"/>
    <config name="minimum_wp_version" value="7.0"/>

    <!-- Modern OOP formatting baseline. -->
    <rule ref="PSR12">
        <!-- The bootstrap and uninstall files legitimately run code beside declarations. -->
        <exclude name="PSR1.Files.SideEffects.FoundWithSymbols"/>
    </rule>

    <!-- Soft line length for readable long sprintf()/translated strings. -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="160"/>
            <property name="absoluteLineLimit" value="0"/>
        </properties>
    </rule>

    <!-- WordPress security + correctness (escaping, nonces, sanitisation, i18n).
         ExceptionNotEscaped is suppressed at the file level (phpcs:disable) in the
         few files that throw with interpolated internal identifiers — Container,
         HookRegistrar, Crypto and Vite — each with a per-file rationale.
         (Those inline disables travel with the code, so the official Plugin Check,
         which ignores this project config, sees them too.) Keeping the sniff enabled
         here flags any NEW unescaped-exception case that lacks a justified disable. -->
    <rule ref="WordPress.Security"/>

    <!-- SQL-injection prevention stays enforced (values use prepare() / %i / %d / %s). -->
    <rule ref="WordPress.DB.PreparedSQL"/>

    <rule ref="WordPress.WP.GlobalVariablesOverride"/>
    <rule ref="WordPress.WP.EnqueuedResources"/>
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="woocommerce-parcelas"/>
            </property>
        </properties>
    </rule>
</ruleset>
